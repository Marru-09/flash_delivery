enum DataLoadStatus {
  /// Whether is initial status.
  initial,

  /// Whether is a failure status.
  failure,

  /// Whether is initial status.
  loading,

  /// Whether is initial status.
  success,
}
